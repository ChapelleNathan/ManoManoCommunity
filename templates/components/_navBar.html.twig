<nav class="navbar navbar-light bg-light">
	<div class="container">
		<a class="navbar-brand" href="/">
			<img src="{{ asset('build/images/logo.png') }}" alt="manomano-logo" class="logo">
		</a>
		<div class="collapse d-flex align-items-baseline" id="navbarNav">
			<ul class="navbar-nav ">
				<li class="nav-item">
					<a class="nav-link active mx-3" href="/posts">Association</a>
				</li>
			</div>
			{% if is_granted('IS_AUTHENTICATED_FULLY') %}
				<div class="nav-item dropdown">
					<button type="button" class="btn btn-light dropdown-toggle d-flex align-items-center p-2 rounded-3" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						<div class="d-flex flex-column align-items-start">
							<p class="m-0">Hello
							</p>
							<p class="m-0 fw-bold">{{ app.user.firstname }}
								{{ app.user.lastname }}</p>
						</div>
					</button>
					<div class="dropdown-menu dropdown-menu-end p-2" aria-labelledby="navbarDropdown">
						<a class="dropdown-item rounded-3" href="{{ path('user_show', {id: app.user.id}) }}">
							<p class="p-2 m-0">
								<i class="bi bi-person me-1"></i>
								My profile</p>
						</a>
						<a class="dropdown-item rounded-3" href="{{ path('post_user_show') }}">
							<p class="p-2 m-0">
								<i class="bi bi-pencil-square me-1"></i>
								My posts</p>
						</a>
						<a class="dropdown-item rounded-3" href="{{ path('app_logout') }}">
							<p class="p-2 m-0">
								<i class="bi bi-box-arrow-left me-1"></i>
								log out</p>
						</a>
					</div>
				</div>
			{% else %}
				<div class="nav-item dropdown">
					<button type="button" class="btn btn-light dropdown-toggle d-flex align-items-center p-2 rounded-3" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						<div class="d-flex flex-column align-items-start">
							<p class="m-0">Bonjour</p>
							<p class="m-0 fw-bold">Identifiez-vous</p>
						</div>
					</button>
					<div class="dropdown-menu dropdown-menu-end p-2" aria-labelledby="navbarDropdown">
						<a class="dropdown-item rounded-3" href="{{ path('login') }}">
							<p class="p-2 m-0">Je suis un particulier</p>
						</a>
						<a class="dropdown-item rounded-3" href="">
							<p class="p-2 m-0">Je suis un pro</p>
						</a>
					</div>
				</div>
			{% endif %}

		</div>
	</nav>
